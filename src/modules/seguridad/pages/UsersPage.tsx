import { useEffect, useState } from 'react';
import { Button, Card, Collapse, Form, Table, Badge } from 'react-bootstrap';
import Swal from 'sweetalert2';
import { FaEye, FaEdit, FaToggleOn, FaToggleOff, FaPlus, FaSearch, FaBroom, FaFilter } from 'react-icons/fa';
import { userService } from '../services/userService';
import { User, UserFilter } from '../models/UserModels';
import UserModal from '../components/UserModal';
import PaginationLite from '../../../shared/components/PaginationLite';
import ActionLink from '../../../shared/components/ActionLink';
import { useAuth } from '../components/AuthContext';
import { PERMISOS } from '../../../shared/utils/permissions';
export default function UsersPage() { const { can } = useAuth(); const [items,setItems]=useState<User[]>([]); const [total,setTotal]=useState(0); const [page,setPage]=useState(1); const [pages,setPages]=useState(0); const [take,setTake]=useState(10); const [filters,setFilters]=useState<UserFilter>({}); const [panel,setPanel]=useState(false); const [selected,setSelected]=useState<User|null>(null); const [modal,setModal]=useState<'new'|'edit'|'detail'|null>(null); const load=async()=>{try{const d=await userService.paginate(page,take,filters);setItems(d.items||[]);setTotal(d.total||0);setPages(d.pages||0);}catch(e:any){Swal.fire('Error',e.friendlyMessage||'No fue posible cargar usuarios.','error')}}; useEffect(()=>{load()},[page,take]); const save=async(p:any)=>{try{selected?await userService.update(selected.id,p):await userService.create(p);await Swal.fire('Correcto','Registro guardado correctamente.','success');await load();}catch(e:any){Swal.fire('Error',e.friendlyMessage||'Error al guardar.','error')}}; const status=async(u:User)=>{const r=await Swal.fire({title:'¿Está seguro?',text:`Se ${u.activo?'inactivará':'activará'} el usuario.`,icon:'warning',showCancelButton:true}); if(!r.isConfirmed)return; try{await userService.changeStatus(u.id,!u.activo); await load();}catch(e:any){Swal.fire('Error',e.friendlyMessage||'Error al cambiar estado.','error')}}; const search=()=>{setPage(1);load()}; const clear=()=>{setFilters({});setPage(1);setTimeout(load,0)}; return <Card className="content-card"><Card.Body><div className="d-flex justify-content-between mb-3"><h4>Gestión Usuarios</h4>{can(PERMISOS.USUARIOS_CREAR)&&<Button onClick={()=>{setSelected(null);setModal('new')}}><FaPlus className="me-2"/>Nuevo usuario</Button>}</div><div className="text-end mb-3"><Button variant="outline-primary" onClick={()=>setPanel(!panel)}><FaFilter className="me-2"/>Filtros</Button></div><Collapse in={panel}><div className="filter-panel mb-3"><div className="row g-2"><div className="col-md-2"><Form.Control placeholder="Código" value={filters.id||''} onChange={e=>setFilters({...filters,id:e.target.value})}/></div><div className="col-md-3"><Form.Control placeholder="Nombre" value={filters.nombre||''} onChange={e=>setFilters({...filters,nombre:e.target.value})}/></div><div className="col-md-3"><Form.Control placeholder="Usuario" value={filters.usuario||''} onChange={e=>setFilters({...filters,usuario:e.target.value})}/></div><div className="col-md-2"><Form.Control placeholder="Correo" value={filters.correo||''} onChange={e=>setFilters({...filters,correo:e.target.value})}/></div><div className="col-md-2"><Form.Select value={filters.activo||''} onChange={e=>setFilters({...filters,activo:e.target.value})}><option value="">Estado</option><option value="true">Activo</option><option value="false">Inactivo</option></Form.Select></div><div className="col-12 text-end"><Button className="me-2" onClick={search}><FaSearch className="me-2"/>Buscar</Button><Button variant="secondary" onClick={clear}><FaBroom className="me-2"/>Limpiar</Button></div></div></div></Collapse><Table responsive hover><thead><tr><th>Código</th><th>Nombre</th><th>Usuario</th><th>Correo</th><th>Rol</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>{items.map(u=><tr key={u.id}><td>{u.id}</td><td>{u.nombre}</td><td>{u.usuario}</td><td>{u.correo}</td><td>{u.rol}</td><td><Badge bg={u.activo?'success':'secondary'}>{u.activo?'Activo':'Inactivo'}</Badge></td><td className="actions-cell"><ActionLink icon={FaEye} label="Ver" color="info" onClick={()=>{setSelected(u);setModal('detail')}}/><ActionLink icon={FaEdit} label="Editar" color="primary" onClick={()=>{setSelected(u);setModal('edit')}}/><ActionLink icon={u.activo?FaToggleOff:FaToggleOn} label={u.activo?'Inactivar':'Activar'} color={u.activo?'warning':'success'} onClick={()=>status(u)}/></td></tr>)}</tbody></Table><PaginationLite page={page} pages={pages} take={take} total={total} onPage={setPage} onTake={(t)=>{setTake(t);setPage(1)}}/><UserModal show={!!modal} onHide={()=>setModal(null)} user={selected} readonly={modal==='detail'} onSave={save}/></Card.Body></Card> }
